import { ICreatorCard } from './CreatorCard';

const base: ICreatorCard = {
  name: 'Creator',
  image: '/mrBeast.jpeg',
  url: '/',
  verified: true,
};

export const mockCreatorCardProps = {
  base,
};
